export class DomainItems
{
    Id:number;
    Name:string;
}

// Define a class in a TypeScript file (e.g., my-class.ts)
// Define a class in a TypeScript file (e.g., my-class.ts)
export class MyDomainItems {
    Id: number;
    Name: string;
  
    constructor(id: number, name: string) {
      this.Id = id;
      this.Name = name;
    }
  }
  

  